{
  "name": "data-structure-visualization",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build:wasm && cd web && npm run dev",
    "build": "npm run build:wasm && cd web && npm run build",
    "build:wasm": "cd crates/visualizer-wasm && wasm-pack build --target web --out-dir ../../web/public/wasm",
    "test:rust": "cargo test",
    "preview": "cd web && npm run preview"
  },
  "devDependencies": {}
}
